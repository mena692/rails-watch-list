<div class="container mt-4">
  <h1>New List</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Create List", class: "btn btn-primary" %>
  <% end %>


  <%= link_to "Back", lists_path, class: "btn btn-secondary mt-3" %>
</div>
